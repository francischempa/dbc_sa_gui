export interface Person {
  per_ID? : string;
  per_Title? : string;
  per_FirstName? : string;
  per_MiddleName? : string;
  per_LastName? : string;
  per_Suffix? : string;
  per_Address1? : string;
  per_Address2? : string;
  per_City? : string;
  per_State? : string;
  per_Zip? : string;
  per_Country? : string;
  per_HomePhone? : string;
  per_WorkPhone? : string;
  per_CellPhone? : string;
  per_Email? : string;
  per_WorkEmail? : string;
  per_BirthMonth? : string;
  per_BirthDay? : string;
  per_BirthYear? : string;
  per_MembershipDate? : string;
  per_Gender? : string;
  per_fmr_ID? : string;
  per_cls_ID? : string;
  per_fam_ID? : string;
  per_Envelope? : string;
  per_DateLastEdited? : string;
  per_DateEntered? : string;
  per_EnteredBy? : string;
  per_EditedBy? : string;
  per_FriendDate? : string;
  per_Flags? : string;
  per_FacebookID? : string;
  per_Twitter? : string;
  per_LinkedIn? : string;
  per_qrcode? : string;
}
